ul.footercontainer
    each menu in ["Home", "Why", "What", "How", "About", "Contact"]
        if (menu == pageName)
          if (menu.indexOf("Home") > -1)
            li.highlight
              a(href="/")= menu
          else
            li.highlight
              a(href="/#{menu}")= menu
        else
          if (menu.indexOf("Home") > -1)
            li
              a(href="/")= menu
          else
            li
              a(href="/#{menu}")= menu
     
ul 
    li
        a(href='/?setLng=en-US')= t('app.english')
    li
        a(href='/?setLng=cn-TW')= t('app.chinese')
    li
        a(href='/?setLng=cl-CO')= t('app.clingon')
